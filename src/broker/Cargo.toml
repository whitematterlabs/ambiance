[package]
name = "pai-broker"
version = "0.1.0"
edition = "2021"
description = "PAI broker — the privileged residue: approvals, audit, fleet view"
license = "Apache-2.0"

[[bin]]
name = "pai-broker"
path = "src/main.rs"

[profile.release]
strip = true
lto = true
